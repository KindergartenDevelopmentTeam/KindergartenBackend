const writer = require('../utils/writer')
const responses = require('../responses')

module.exports = {
    uploadPicture: (req, res, next) => {
        const pictureData = req.swagger.params.picture.value


    }
}